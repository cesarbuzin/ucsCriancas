<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="criancas.ucs.com.projetocriancas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".fragment.ListagemActivity"
            android:label="@string/title_activity_listagem"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".fragment.ViewCriancasActivity"
            android:label="@string/title_activity_view_criancas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".fragment.EditDadosActivity" />
        <activity android:name=".fragment.IncludeInfoActivity" />
        <activity
            android:name=".fragment.GraficoActivity"
            android:label="@string/title_activity_grafico"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.CriancasFragmentActivity"></activity>
    </application>

</manifest>